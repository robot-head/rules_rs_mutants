bcr_test_module:
  module_path: "."
  matrix:
    platform: ["debian10", "ubuntu2004"]
    bazel: ["8.x", "9.x"]
  tasks:
    run_tests:
      name: "Run tests"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
